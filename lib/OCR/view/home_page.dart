import 'package:flutter/material.dart';

class OCR_Home extends StatelessWidget {
  const OCR_Home({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("OCR Project"),
      ),
      body: Column(
        children: const [

        ],
      ),
    );
  }
}
